{
  "name": "sve-nest",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/api/dist/main"
  },
  "devDependencies": {
    "turbo": "^1.10.14"
  },
  "workspaces": [
    "apps/*"
  ]
}
